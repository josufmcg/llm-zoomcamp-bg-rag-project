# Task 004 — Embedding Service

## Objective

Create the `embedder.py` module that wraps the local ONNX embedding model (`Xenova/all-MiniLM-L6-v2`) for encoding text into 384-dimensional vectors. This module is used by both the ingestion pipeline and the search engine.

## Dependencies

- **Task 001** (Project Setup) must be complete — needs ONNX model downloaded to `models/Xenova/all-MiniLM-L6-v2/`.

## Files to Create

| File | Purpose |
|------|---------|
| `src/bg_rag/embedder.py` | ONNX embedding model wrapper |

## Reference

This module is adapted from the reference project's `llm-zoomcamp/02.vector/embedder.py`. The logic is identical; only the import path and typing annotations are updated.

## Detailed Instructions

### Step 1: Create `src/bg_rag/embedder.py`

```python
"""ONNX-based text embedding model wrapper.

Uses the Xenova/all-MiniLM-L6-v2 ONNX model for encoding text
into 384-dimensional normalized vectors. Performs mean pooling
over token embeddings with attention mask, followed by L2 normalization.

Usage:
    embedder = Embedder("models/Xenova/all-MiniLM-L6-v2")
    vector = embedder.encode("What is a Fighter?")  # shape: (384,)
    vectors = embedder.encode_batch(["text1", "text2"])  # shape: (2, 384)
"""

import numpy as np
import onnxruntime as ort
from pathlib import Path
from tokenizers import Tokenizer


class Embedder:
    """Text embedding model using ONNX Runtime.

    Loads a tokenizer and ONNX model from a local directory.
    Produces 384-dimensional L2-normalized embeddings.

    Args:
        path: Path to the directory containing tokenizer.json and model.onnx.
    """

    def __init__(self, path: str | Path = "models/Xenova/all-MiniLM-L6-v2") -> None:
        path = Path(path)
        if not (path / "tokenizer.json").exists():
            raise FileNotFoundError(
                f"tokenizer.json not found in {path}. "
                "Run 'uv run python scripts/download_model.py' first."
            )
        if not (path / "model.onnx").exists():
            raise FileNotFoundError(
                f"model.onnx not found in {path}. "
                "Run 'uv run python scripts/download_model.py' first."
            )

        self.tokenizer = Tokenizer.from_file(str(path / "tokenizer.json"))
        self.session = ort.InferenceSession(
            str(path / "model.onnx"),
            providers=["CPUExecutionProvider"],
        )
        self.input_names = {inp.name for inp in self.session.get_inputs()}

    def encode(self, text: str, normalize: bool = True) -> np.ndarray:
        """Encode a single text string into a vector.

        Args:
            text: The text to encode.
            normalize: If True, L2-normalize the output vector.

        Returns:
            A 1-D numpy array of shape (384,).
        """
        return self.encode_batch([text], normalize=normalize)[0]

    def encode_batch(self, texts: list[str], normalize: bool = True) -> np.ndarray:
        """Encode multiple texts into vectors.

        Args:
            texts: List of text strings to encode.
            normalize: If True, L2-normalize each output vector.

        Returns:
            A 2-D numpy array of shape (len(texts), 384).
        """
        self.tokenizer.enable_padding()
        encoded = self.tokenizer.encode_batch(texts)

        feed: dict[str, np.ndarray] = {}
        if "input_ids" in self.input_names:
            feed["input_ids"] = np.array(
                [e.ids for e in encoded], dtype=np.int64
            )
        if "attention_mask" in self.input_names:
            feed["attention_mask"] = np.array(
                [e.attention_mask for e in encoded], dtype=np.int64
            )
        if "token_type_ids" in self.input_names:
            feed["token_type_ids"] = np.array(
                [e.type_ids for e in encoded], dtype=np.int64
            )

        # Run ONNX model inference
        hidden = self.session.run(None, feed)[0]  # shape: (batch, seq_len, 384)

        # Mean pooling: average over non-padding tokens
        mask = feed["attention_mask"][..., None]  # shape: (batch, seq_len, 1)
        pooled = (hidden * mask).sum(axis=1) / mask.sum(axis=1)  # shape: (batch, 384)

        # L2 normalization
        if normalize:
            norms = np.linalg.norm(pooled, axis=1, keepdims=True)
            pooled = pooled / norms

        return pooled
```

### Step 2: Verify the model path

Ensure the ONNX model files exist:

```bash
ls models/Xenova/all-MiniLM-L6-v2/
```

Expected: `model.onnx  tokenizer.json`

If not present, run:

```bash
uv run python scripts/download_model.py
```

## Verification Checklist

1. **Module imports successfully:**
   ```bash
   uv run python -c "from bg_rag.embedder import Embedder; print('Import OK')"
   ```
   Expected: `Import OK`

2. **Single text encoding works:**
   ```bash
   uv run python -c "
   from bg_rag.embedder import Embedder
   e = Embedder()
   v = e.encode('What is a Fighter class?')
   print(f'Shape: {v.shape}')
   print(f'Dtype: {v.dtype}')
   print(f'Norm: {(v @ v) ** 0.5:.4f}')
   print(f'First 5: {v[:5]}')
   "
   ```
   Expected:
   - Shape: `(384,)`
   - Dtype: `float32` (or `float64`)
   - Norm: approximately `1.0000` (since L2-normalized)

3. **Batch encoding works:**
   ```bash
   uv run python -c "
   from bg_rag.embedder import Embedder
   e = Embedder()
   texts = ['What is a Fighter?', 'Tell me about the Mage class', 'How do Thieves work?']
   v = e.encode_batch(texts)
   print(f'Shape: {v.shape}')
   assert v.shape == (3, 384), f'Expected (3, 384), got {v.shape}'
   print('Batch encoding OK')
   "
   ```
   Expected: `Shape: (3, 384)` and `Batch encoding OK`

4. **Semantic similarity works (same topic closer than different topic):**
   ```bash
   uv run python -c "
   from bg_rag.embedder import Embedder
   import numpy as np
   e = Embedder()
   v1 = e.encode('What is the Fighter class?')
   v2 = e.encode('Tell me about warriors and fighters')
   v3 = e.encode('How to cook pasta')
   sim_related = float(np.dot(v1, v2))
   sim_unrelated = float(np.dot(v1, v3))
   print(f'Fighter vs Warriors: {sim_related:.4f}')
   print(f'Fighter vs Pasta:    {sim_unrelated:.4f}')
   assert sim_related > sim_unrelated, 'Related texts should have higher similarity'
   print('Semantic similarity OK')
   "
   ```
   Expected: The fighter/warriors similarity should be higher than fighter/pasta.

5. **Missing model raises clear error:**
   ```bash
   uv run python -c "
   try:
       from bg_rag.embedder import Embedder
       e = Embedder('/nonexistent/path')
       print('ERROR: Should have raised FileNotFoundError')
   except FileNotFoundError as ex:
       print(f'Good: {ex}')
   "
   ```
   Expected: A `FileNotFoundError` with a helpful message about running `download_model.py`.
