# Task 006 — Search Engine

## Objective

Create the `search.py` module implementing three search methods over the PostgreSQL `documents` table: vector search (cosine similarity via pgvector), keyword search (full-text with ts_vector), and hybrid search (Reciprocal Rank Fusion combining both).

## Dependencies

- **Task 003** (Database Schema) — needs `db.py` and the `documents` table with `text_search` tsvector column and trigger.
- **Task 004** (Embedding Service) — needs `embedder.py` for query vectorization.
- **Task 005** (Document Ingestion) — needs documents in the database to search.

## Files to Create

| File | Purpose |
|------|---------|
| `src/bg_rag/search.py` | Vector, keyword, and hybrid search functions |

## Search Methods Overview

### Vector Search
- Encode the query text into a 384-dim vector using the Embedder.
- Find nearest neighbors using pgvector's `<=>` operator (cosine distance).
- Score = `1 - cosine_distance` (so higher = more similar).

### Keyword Search
- Use PostgreSQL's `plainto_tsquery('english', query)` to parse the query.
- Rank results using `ts_rank()` against the pre-computed `text_search` tsvector column.
- The `text_search` column has weights: subcategory=A, question=B, text=C (set by the trigger in db.py).

### Hybrid Search (Reciprocal Rank Fusion)
- Run both vector search and keyword search independently.
- Combine using RRF: `rrf_score(doc) = Σ 1/(k + rank_i)` where `k=60`.
- This method balances semantic similarity with keyword relevance.
- Return results sorted by combined RRF score.

## Detailed Instructions

### Step 1: Create `src/bg_rag/search.py`

```python
"""Search engine with vector, keyword, and hybrid search methods.

All search methods query the PostgreSQL documents table and return
results as a list of dicts with document fields plus a relevance score.

Usage:
    from bg_rag.search import SearchEngine
    from bg_rag.embedder import Embedder

    embedder = Embedder()
    engine = SearchEngine(embedder)
    results = engine.hybrid_search("What class should I pick for melee combat?")
"""

from bg_rag.db import get_db_connection
from bg_rag.embedder import Embedder


class SearchEngine:
    """Search engine supporting vector, keyword, and hybrid search.

    Args:
        embedder: Initialized Embedder instance for query vectorization.
    """

    def __init__(self, embedder: Embedder) -> None:
        self.embedder = embedder

    def vector_search(self, query: str, limit: int = 5) -> list[dict]:
        """Search using cosine similarity on vector embeddings.

        Args:
            query: The search query text.
            limit: Maximum number of results to return.

        Returns:
            List of document dicts with an added 'score' field (0-1, higher is better).
        """
        query_embedding = self.embedder.encode(query)

        conn = get_db_connection()
        try:
            with conn.cursor() as cur:
                cur.execute(
                    """
                    SELECT
                        id, doc_id, category, subcategory, question, text,
                        1 - (embedding <=> %s::vector) AS score
                    FROM documents
                    ORDER BY embedding <=> %s::vector
                    LIMIT %s
                    """,
                    (query_embedding.tolist(), query_embedding.tolist(), limit),
                )
                results = cur.fetchall()
        finally:
            conn.close()

        return [dict(row) for row in results]

    def keyword_search(self, query: str, limit: int = 5) -> list[dict]:
        """Search using PostgreSQL full-text search with ranking.

        Uses the pre-computed text_search tsvector column with
        weighted fields (subcategory=A, question=B, text=C).

        Args:
            query: The search query text.
            limit: Maximum number of results to return.

        Returns:
            List of document dicts with an added 'score' field (higher is better).
        """
        conn = get_db_connection()
        try:
            with conn.cursor() as cur:
                cur.execute(
                    """
                    SELECT
                        id, doc_id, category, subcategory, question, text,
                        ts_rank(text_search, plainto_tsquery('english', %s)) AS score
                    FROM documents
                    WHERE text_search @@ plainto_tsquery('english', %s)
                    ORDER BY score DESC
                    LIMIT %s
                    """,
                    (query, query, limit),
                )
                results = cur.fetchall()
        finally:
            conn.close()

        return [dict(row) for row in results]

    def hybrid_search(self, query: str, limit: int = 5, rrf_k: int = 60) -> list[dict]:
        """Search using Reciprocal Rank Fusion of vector + keyword search.

        Runs both vector and keyword search, then combines results using
        RRF: score(doc) = sum(1 / (k + rank_i)) for each ranking the
        document appears in.

        Args:
            query: The search query text.
            limit: Maximum number of results to return.
            rrf_k: The RRF constant (default 60, standard value).

        Returns:
            List of document dicts with an added 'score' field (RRF score).
        """
        # Get more results from each method to ensure good fusion
        fetch_limit = limit * 3

        vector_results = self.vector_search(query, limit=fetch_limit)
        keyword_results = self.keyword_search(query, limit=fetch_limit)

        # Build RRF scores
        # Key: doc_id (the database primary key), Value: rrf_score
        rrf_scores: dict[int, float] = {}
        doc_data: dict[int, dict] = {}

        for rank, doc in enumerate(vector_results):
            doc_pk = doc["id"]
            rrf_scores[doc_pk] = rrf_scores.get(doc_pk, 0.0) + 1.0 / (rrf_k + rank + 1)
            doc_data[doc_pk] = doc

        for rank, doc in enumerate(keyword_results):
            doc_pk = doc["id"]
            rrf_scores[doc_pk] = rrf_scores.get(doc_pk, 0.0) + 1.0 / (rrf_k + rank + 1)
            doc_data[doc_pk] = doc

        # Sort by RRF score descending
        sorted_ids = sorted(rrf_scores.keys(), key=lambda pk: rrf_scores[pk], reverse=True)

        results = []
        for pk in sorted_ids[:limit]:
            doc = doc_data[pk].copy()
            doc["score"] = rrf_scores[pk]
            results.append(doc)

        return results

    def search(self, query: str, method: str = "hybrid", limit: int = 5) -> list[dict]:
        """Dispatch search to the specified method.

        Args:
            query: The search query text.
            method: One of "vector", "keyword", or "hybrid".
            limit: Maximum number of results to return.

        Returns:
            List of document dicts with scores.

        Raises:
            ValueError: If method is not recognized.
        """
        if method == "vector":
            return self.vector_search(query, limit=limit)
        elif method == "keyword":
            return self.keyword_search(query, limit=limit)
        elif method == "hybrid":
            return self.hybrid_search(query, limit=limit)
        else:
            raise ValueError(
                f"Unknown search method: {method}. "
                "Use 'vector', 'keyword', or 'hybrid'."
            )
```

## Verification Checklist

**Prerequisites:** PostgreSQL must be running, tables initialized, and documents ingested (Tasks 003-005 complete).

1. **Module imports successfully:**
   ```bash
   uv run python -c "from bg_rag.search import SearchEngine; print('Import OK')"
   ```
   Expected: `Import OK`

2. **Vector search returns results:**
   ```bash
   uv run python -c "
   from bg_rag.embedder import Embedder
   from bg_rag.search import SearchEngine
   e = Embedder()
   s = SearchEngine(e)
   results = s.vector_search('best class for melee combat')
   for r in results[:3]:
       print(f'{r[\"doc_id\"]:12s} | score={r[\"score\"]:.4f} | {r[\"subcategory\"]}')
   assert len(results) > 0, 'Should return results'
   print(f'Total: {len(results)} results')
   "
   ```
   Expected: Several results with scores, likely Fighter/Barbarian/Paladin near the top.

3. **Keyword search returns results:**
   ```bash
   uv run python -c "
   from bg_rag.embedder import Embedder
   from bg_rag.search import SearchEngine
   e = Embedder()
   s = SearchEngine(e)
   results = s.keyword_search('backstab stealth thief')
   for r in results[:3]:
       print(f'{r[\"doc_id\"]:12s} | score={r[\"score\"]:.4f} | {r[\"subcategory\"]}')
   assert len(results) > 0, 'Should return results'
   print(f'Total: {len(results)} results')
   "
   ```
   Expected: Thief should be near the top.

4. **Hybrid search returns results:**
   ```bash
   uv run python -c "
   from bg_rag.embedder import Embedder
   from bg_rag.search import SearchEngine
   e = Embedder()
   s = SearchEngine(e)
   results = s.hybrid_search('magic spells wizard')
   for r in results[:3]:
       print(f'{r[\"doc_id\"]:12s} | score={r[\"score\"]:.6f} | {r[\"subcategory\"]}')
   assert len(results) > 0, 'Should return results'
   print(f'Total: {len(results)} results')
   "
   ```
   Expected: Mage/Sorcerer should be near the top.

5. **Dispatch method works:**
   ```bash
   uv run python -c "
   from bg_rag.embedder import Embedder
   from bg_rag.search import SearchEngine
   e = Embedder()
   s = SearchEngine(e)
   for method in ['vector', 'keyword', 'hybrid']:
       results = s.search('fighter', method=method)
       print(f'{method:8s}: {len(results)} results, top={results[0][\"doc_id\"]}')
   print('All methods OK')
   "
   ```
   Expected: All three methods return results.

6. **Invalid method raises error:**
   ```bash
   uv run python -c "
   from bg_rag.embedder import Embedder
   from bg_rag.search import SearchEngine
   e = Embedder()
   s = SearchEngine(e)
   try:
       s.search('test', method='invalid')
       print('ERROR: Should have raised ValueError')
   except ValueError as ex:
       print(f'Good: {ex}')
   "
   ```
   Expected: `ValueError` with helpful message.

7. **Results have expected fields:**
   ```bash
   uv run python -c "
   from bg_rag.embedder import Embedder
   from bg_rag.search import SearchEngine
   e = Embedder()
   s = SearchEngine(e)
   results = s.vector_search('cleric')
   r = results[0]
   expected_fields = {'id', 'doc_id', 'category', 'subcategory', 'question', 'text', 'score'}
   actual_fields = set(r.keys())
   assert expected_fields.issubset(actual_fields), f'Missing fields: {expected_fields - actual_fields}'
   print('All expected fields present')
   "
   ```
   Expected: `All expected fields present`
